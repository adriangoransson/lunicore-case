# Case for Lunicore

To get started:
    - Setup a virtualenv (python3) in venv
        virtualenv -p python3 venv
    - Install dependencies
        venv/bin/pip install -r requirements.txt
    - Setup database
        venv/bin/python3 database_reset.py

To run:
    ./start-server

Todo:
    - Frontend
    - Access levels
    - Registration
